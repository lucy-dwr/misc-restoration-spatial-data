# Proposition Name Canonicalization 2026-07-20

`scripts/canonicalize-proposition-name.R` replaces standalone `Prop` with
`Proposition` in standardized text fields and the funding-source review CSV.
The replacement does not affect longer words that merely contain those letters.
Raw submissions remain unchanged.
